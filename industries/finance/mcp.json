{
  "mcpServers": {
    "stripe": {
      "command": "npx",
      "args": ["-y", "@stripe/mcp", "--tools=all"],
      "env": {
        "STRIPE_SECRET_KEY": "{{STRIPE_SECRET_KEY}}"
      }
    },
    "excel": {
      "command": "npx",
      "args": ["-y", "@negokaz/excel-mcp-server"]
    }
  }
}
